# P2-M1-P2-2-Aggregate-stats.py
import numpy as np

# 2. Aggregate stats Using the same array, print the .sum(), .mean(), .max(), and .min().
data = np.array([3, 5, 2, 4, 6])
print(data.sum())
print(data.mean())
print(data.max())
print(data.min())
